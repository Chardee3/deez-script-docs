# Configuration

All primary settings are managed through the resource's `config.lua` file.

The final Deez Chat V1 configuration options and their descriptions will be added to this page before release.

> Do not rename variables inside the config file. Only change supported values.
